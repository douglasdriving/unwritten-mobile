import { Text, View } from 'react-native';
import { styles } from '../../style';

export const OpenRoom = () => {
  return(
    <View style={styles.container}>
      <Text style={styles.h1}>Open a New Room</Text>
    </View>
  );
}